export interface followResponse {
    id: string;
    followerId: string;
    followerUsername: string;
    followedId: string;
    followedUsername: string;
    status: string;
    avatarUrl: string;
    createdAt: string;
}
